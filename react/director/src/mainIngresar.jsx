import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import { App } from './appIngresar.jsx'

const root = createRoot(document.getElementById('root'))

root.render(
    < App />
)
