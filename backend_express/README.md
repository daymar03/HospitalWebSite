## Iniciar y crear la base de datos
sudo systemctl start mysql && sudo mysql < create_db.sql && sudo mysql < dummy.sql

## Instalar las dependencias
npm install 

## Iniciar el servidor
sudo npm run dev

## Rutas disponibles

